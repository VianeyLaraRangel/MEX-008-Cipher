
/*Aquí debes escuchar eventos del DOM, invocar cipher.encode() o cipher.decode() según sea necesario y actualizar el resultado en la UI.*/

//Obtener número de desplazamiento (offset o key) y guardárlo en una varible
//let offset = document.getElementById('offset-box').value;
//console.log('offset');

//console.log('offset-box');

//Detonar el evento del input de desplazamiento (offset o key) y pasarle la función que convierte el offset en un entero.
//offset.addEventListener('click', offsetParse());

